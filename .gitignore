node_modules/
dist/
*.log
.DS_Store
out/
*.pdf
.slaide-cache/

# Local agent/editor config — never commit (may carry local settings)
.claude/

# Mirror-export staging tree (regenerated by private/tools/mirror-export.ts)
.mirror-stage/

# Rust viewer build artifacts + the bundled engine binary (built via `npm run build:viewer`)
viewer/target/
viewer/vendor/
# Imported PowerPoint fixtures + license keys (kept local; never committed, never public)
# Globbed everywhere so moving fixtures between folders can't accidentally un-ignore them.
*.pptx
*.key
